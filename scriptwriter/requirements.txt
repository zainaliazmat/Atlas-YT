# Marlow, The Scriptwriter — dependencies.

python-dotenv         # loads keys from your local .env
claude-agent-sdk      # DEFAULT brain (engine) + the co-worker chat seam (converse), via your subscription
requests              # only needed if you switch MARLOW_LLM to "deepseek" (raw HTTP, no openai dep)

# Optional — only if you switch MARLOW_LLM to "gemini":
# google-generativeai

# (MARLOW_LLM=deepseek needs no extra package — it uses raw `requests` above.)
