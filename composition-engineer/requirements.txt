# Mason, The Composition Engineer — dependencies.

python-dotenv         # loads keys from your local .env
claude-agent-sdk      # DEFAULT brain for the co-worker chat seam (converse), via your subscription
requests              # only needed if you switch MASON_LLM to "deepseek" (raw HTTP, no openai dep)

# Optional — only if you switch MASON_LLM to "gemini":
# google-generativeai

# (MASON_LLM=deepseek needs no extra package — it uses raw `requests` above.)
#
# The RENDER engine is HyperFrames, run via the CLI (not a Python dep):
#   - Node.js >= 22 + FFmpeg on PATH
#   - `npx skills add heygen-com/hyperframes` (installs the doc skills; the CLI is the
#     `hyperframes` npm package, invoked as `npx hyperframes …`)
