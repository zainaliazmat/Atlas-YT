# Sage, The Researcher — dependencies.

requests              # HTTP for Wikipedia / GDELT / page fetching + the DeepSeek brain
ddgs                  # DuckDuckGo web search — FREE, no API key (default web seam)
python-dotenv         # loads keys from your local .env
claude-agent-sdk      # DEFAULT brain (chat) + the co-worker chat seam (converse), via your subscription

# Optional — only if you switch SAGE_LLM to "gemini":
# google-generativeai

# (SAGE_LLM=deepseek needs no extra package — it uses raw `requests` above.)

# Optional page-extraction upgrade (see search.fetch_text):
# trafilatura
