# Iris, The Art Director — dependencies.

python-dotenv         # loads keys from your local .env
claude-agent-sdk      # DEFAULT brain (engine) + the co-worker chat seam (converse), via your subscription
requests              # only needed if you switch IRIS_LLM to "deepseek" (raw HTTP, no openai dep)

# Optional — only if you switch IRIS_LLM to "gemini":
# google-generativeai

# (IRIS_LLM=deepseek needs no extra package — it uses raw `requests` above.)
